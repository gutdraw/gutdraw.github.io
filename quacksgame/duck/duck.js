/* eslint-disable require-yield, eqeqeq */

import {
  Sprite,
  Trigger,
  Watcher,
  Costume,
  Color,
  Sound
} from "https://unpkg.com/leopard@^1/dist/index.esm.js";

export default class _96967876PictureLibraryDownloadDuckSvgPngIconFree extends Sprite {
  constructor(...args) {
    super(...args);

    this.costumes = [
      new Costume(
        "96-967876_picture-library-download-duck-svg-png-icon-free",
        "./duck/costumes/duck.png",
        { x: 365.5, y: 360 }
      )
    ];

    this.sounds = [];

    this.triggers = [];
  }
}
